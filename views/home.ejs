<%- include('partials/header'); -%>
<h1>Home</h1>
<%= homeStartingContent %>

<% posts.forEach(function(post) { %>
    <h2><%= post.title %></h2>
    <p><%= post.entry %></p>
    <% }) %>

<%- include('partials/footer'); -%>